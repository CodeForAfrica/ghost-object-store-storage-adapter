{
  "name": "ghost-object-store-storage-adapter",
  "version": "1.0.0",
  "description": "Object Store storage adapter for Ghost CMS. Tested and works with MinIO and Amazon S3.",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch --verbose",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "ghost",
    "s3",
    "minio",
    "storage",
    "adapter"
  ],
  "author": "Mark Ng'ang'a",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.920.0",
    "ghost-storage-base": "^1.1.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-extended": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
